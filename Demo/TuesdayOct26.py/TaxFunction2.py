def calculateTax(price):
    result = price * 1.15
    return result


productPrice = float(input("Enter the product price"))
totalPrice = calculateTax(productPrice)
print("The Product's price after tax is {0:.2f}".format(totalPrice))

productPrice = float(input("Enter the product price"))
totalPrice = calculateTax(productPrice)
print("The Product's price after tax is {0:.2f}".format(totalPrice))


productPrice = float(input("Enter the product price"))
totalPrice = calculateTax(productPrice)
print("The Product's price after tax is {0:.2f}".format(totalPrice))
